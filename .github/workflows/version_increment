name: Release Branch Versioning
on:
#   workflow_dispatch:
  pull_request:
    types:
      - closed
    branches:
      - main


jobs:
  check_if_merged:
    if: ${{ github.event.pull_request.merged == true }}
    runs-on: ubuntu-latest
    steps:
